apiVersion: v1
kind: Service
metadata:
  name: openclaw-headless
  namespace: openclaw-prod
spec:
  clusterIP: None
  selector:
    app: openclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: gateway
    - name: bridge
      port: 18790
      targetPort: bridge
