apiVersion: v1
kind: Service
metadata:
  name: oci-anthropic-gateway
  namespace: gateway-prod
spec:
  type: ClusterIP
  selector:
    app: oci-anthropic-gateway
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
